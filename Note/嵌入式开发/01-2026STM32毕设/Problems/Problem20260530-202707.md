## 任务目标

在 `03-硬件连接/02-答疑解惑.md` 中补充 STM32 硬件连接答疑，重点解释电源、电平、通信协议、引脚方向、上拉/下拉、STM32 引脚可用性、外设功能以及 Reference Manual 中常见章节的用途。

## 思考过程

假设本文面向刚开始做 STM32 毕设硬件连接的读者，以当前项目常见组合 `STM32F103C8T6 小系统板 + 北斗/GPS 模块 + OLED` 为主要示例，但内容尽量保持通用。

文档不应只给结论，而应解释“为什么要这样判断”和“怎么看资料”。因此采用逐问逐答结构，每个问题都包含判断依据、常见错误、连接规则和 CubeMX/软件配置方向。

## 遇到的问题

目标文件 `03-硬件连接/02-答疑解惑.md` 当前为空，需要从零组织内容。

同目录已有文档为 UTF-8 编码，PowerShell 默认读取时出现乱码，需要显式使用 UTF-8 读取确认内容。

## 解决方法

新建答疑文档，按用户列出的 8 个问题扩展为 9 个章节：

1. 三条硬件连接原则。
2. IO 电平如何判断。
3. 常见通信方式及接法。
4. 引脚方向判断方法。
5. 上拉/下拉判断方法。
6. STM32 引脚硬件可用性判断。
7. STM32 内部外设功能与连接决策。
8. 总线、寄存器、时钟树、中断、DMA、Boot 模式说明。
9. 实际接线前检查流程。

没有引入新工具或新项目结构，只修改目标 Markdown 并新增本次 Problems 记录。

## 验证结果

已重新以 UTF-8 读取目标文档，确认标题和章节正常显示。

验证结果：

- `03-硬件连接/02-答疑解惑.md` 当前共 1413 行。
- 章节覆盖用户提出的电源、电平、通信方式、引脚方向、上拉/下拉、STM32 引脚可用性、外设功能、总线/寄存器/时钟树/中断/DMA/Boot 模式等问题。
- `Problems/Problem20260530-202707.md` 已创建并记录本次修改。

## 代码变更摘要

- `03-硬件连接/02-答疑解惑.md`：新增 STM32 硬件连接答疑正文。
- `Problems/Problem20260530-202707.md`：新增本次文档修改记录。
