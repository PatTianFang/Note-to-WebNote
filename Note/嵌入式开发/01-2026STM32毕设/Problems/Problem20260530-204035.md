## 任务目标

在 `04-软件设计/01-总体思路.md` 中详细说明 `00-张立超毕设/04-各模块连接的烧录代码/01-北斗显示屏STM32连接` 工程的整体设计思路，包括软件架构、硬件搭配、模块选型原因、接口选择、数据流、解析逻辑、显示逻辑、设计取舍、优点和排查思路。

## 思考过程

先读取目标 Markdown，确认当前文件为空。随后查看目标工程的核心源码和配置，重点包括：

- `Core/Src/main.c`
- `Core/Src/bds_nmea.c`
- `Core/Src/bds_display.c`
- `Core/Src/oled_spi.c`
- `Core/Inc/*.h`
- `Core/Src/stm32f1xx_hal_msp.c`
- `Core/Src/stm32f1xx_it.c`
- `README.md`
- `VSCode.ioc`

文档应以实际代码实现为依据，而不是只做泛泛的软件设计说明。当前代码实际使用 `USART2` 的 `PA2/PA3` 连接北斗/GPS 模块，OLED 使用 `PB8/PB9/PB12/PB13` GPIO 模拟 SPI。需要在文档里明确说明 `.ioc` 仍残留 USART1 配置，但当前固件以源码和 README 中的 USART2 实现为准。

## 遇到的问题

目标文档为空，需要从零组织内容。

工程中 `.ioc` 配置显示 USART1/PA9/PA10，但源码 `main.c`、`stm32f1xx_hal_msp.c`、README 均说明实际接线和运行逻辑使用 USART2/PA2/PA3。这个差异容易让读者混淆，因此文档中需要主动指出。

## 解决方法

计划写入一篇完整的软件总体设计文档，包含：

1. 项目目标和总体方案。
2. 硬件与软件的对应关系。
3. 为什么 STM32F103C8T6 适合这个项目。
4. 为什么北斗/GPS 用 UART/USART2。
5. 为什么 OLED 用 GPIO 模拟 SPI。
6. 代码模块分层和职责。
7. 中断接收、主循环解析、OLED 刷新的数据流。
8. NMEA/RMC 解析、坐标转换、北京时间转换。
9. 稳定性、资源占用、可维护性方面的设计取舍。
10. 常见故障和后续扩展方向。

## 验证结果

已重新以 UTF-8 读取目标文件，确认标题、章节和关键设计点存在。

验证结果：

- `04-软件设计/01-总体思路.md` 当前共 1306 行。
- 章节覆盖项目定义、硬件和软件搭配、STM32 选型、USART2 选用、OLED GPIO 模拟 SPI、分层架构、中断接收、NMEA/RMC 解析、显示设计、时钟和外设配置、设计好处、限制风险、调试顺序和后续扩展。
- 已明确记录 `.ioc` 中 USART1 配置与当前源码 USART2 实现不一致的问题，避免后续读者混淆。

## 代码变更摘要

- `04-软件设计/01-总体思路.md`：新增软件设计总体说明。
- `Problems/Problem20260530-204035.md`：新增本次文档修改记录。
