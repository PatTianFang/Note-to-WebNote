# Problem20260530-155638

## 任务目标

根据用户要求，移除 `01-环境准备/02-采取方案.md` 中关于第三方 STM32 VS Code 插件的方案内容，改为使用 STM32CubeProgrammer 将编译好的程序烧录进入单片机。

## 思考过程

- 原文大部分章节围绕第三方 Makefile 插件展开，包括插件关系、Makefile 工程、插件初始化、插件烧录和调试，因此局部删除会留下大量上下文冲突。
- 用户明确要求“去掉该部分，使用刚刚提到的方法”，所以新方案应以 STM32CubeProgrammer GUI/CLI 烧录为核心。
- 保留 VS Code 和 ST 官方 STM32Cube 插件，但工程路线改为 CMake，更符合 ST 官方插件方向。
- 新手最需要分清编译产物 `.elf`、`.hex`、`.bin` 的区别，尤其是 `.bin` 需要填写 `0x08000000` 起始地址。

## 遇到的问题

- 原文中旧插件相关内容分布在标题、方案说明、插件关系、安装清单、目录结构、工程创建、构建下载调试、常见问题、检查清单和资料入口中。
- 如果只删除单个章节，会导致后文仍然引用旧插件、Makefile 和旧配置文件。

## 解决方法

- 直接重写 `02-采取方案.md`，统一为 `STM32CubeMX + VS Code + ST 官方 STM32Cube 插件 + STM32CubeProgrammer` 方案。
- 新增 STM32CubeProgrammer 图形界面烧录步骤。
- 新增 STM32CubeProgrammer CLI 烧录命令。
- 新增 VS Code `tasks.json` 调用 CLI 的示例。
- 保留 F103C8T6、SWD、BOOT0、`0x08000000`、LED/串口验证等初学者关键内容。

## 验证结果

- 已确认 `01-环境准备/02-采取方案.md` 文件存在，大小约 18 KB。
- 已检查目标文档中旧插件名称、扩展标识和旧配置文件名的出现次数为 0。
- 已确认文档包含 STM32CubeProgrammer 图形界面烧录步骤，包括 `Erasing & Programming`、`Start Programming`、`.bin` 地址 `0x08000000`。
- 已确认文档包含 STM32CubeProgrammer CLI 烧录命令和 VS Code `tasks.json` 调用 CLI 的示例。
- 已确认主文档包含 17 个二级章节，覆盖方案结论、软件安装、CMake 工程创建、编译产物选择、GUI/CLI 烧录、调试区别、验证流程和常见问题。

## 代码变更摘要

- `01-环境准备/02-采取方案.md`：重写为 ST 官方插件 + STM32CubeProgrammer 烧录方案。
- `Problems/Problem20260530-155638.md`：新增本次文档修改记录。
